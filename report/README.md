# Report

`report.tex` is the LaTeX source for the final report. `report.pdf` mirrors the
same content for upload preview. `benchmark.csv` and `benchmark.pdf` are
generated by:

```bash
kadai-sort benchmark --output report
```

Use a LaTeX-enabled environment to recompile `report.tex` when a fresh
LaTeX-built PDF is required.
